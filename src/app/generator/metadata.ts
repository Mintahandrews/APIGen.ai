import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'API Client Generator',
  description: 'Generate production-ready API clients from OpenAPI specifications in multiple languages',
};
